version: "2"
sql:
  - engine: "sqlite"
    queries: "data/db/query"
    schema: "data/db/migrations"
    gen:
      go:
        package: "db"
        out: "data/db/out"
        emit_json_tags: true
        emit_interface: true #generate queries interface